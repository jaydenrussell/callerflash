# electron-builder config for CallerFlash

appId: com.callerflash.app
productName: CallerFlash

directories:
  output: release
  buildResources: buildResources

files:
  - dist/**/*
  - electron/**/*
  - "!**/*.map"
  - "!**/*.tsbuildinfo"
  - "!**/.DS_Store"
  - "!node_modules/**/*"

artifactName: "CallerFlash-${version}.${ext}"

# Pack app code into asar for faster cold start + smaller installer.
# Compression: maximum saves ~15% on installer size with negligible
# startup overhead on modern CPUs.
asar: true
asarUnpack:
  - buildResources/**

win:
  publisherName: CallerFlash
  icon: app.ico
  target:
    - target: nsis
      arch:
        - x64
  artifactName: "CallerFlash-Setup-${version}.${ext}"

nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: CallerFlash
  installerIcon: cflogo.ico
  uninstallerIcon: cflogo.ico
  installerHeaderIcon: cflogo.ico
  runAfterFinish: true

# Publish config — required for electron-builder to generate the
# `latest.yml` / `beta.yml` / `nightly.yml` update manifests consumed
# by electron-updater at install time. Actual publishing is performed
# by the workflow via softprops/action-gh-release, NOT by electron-builder
# itself (the workflow runs electron-builder with --publish never).
publish:
  provider: github
  owner: jaydenrussell
  repo: CallerFlash

extraResources:
  - from: buildResources/cflogo.ico
    to: cflogo.ico
  - from: buildResources/cflogo.png
    to: cflogo.png
  - from: buildResources/app.ico
    to: app.ico
