name: Sync main → alpha

# Automatically pushes the latest main to the alpha branch on every
# commit. Uses a PAT (not GITHUB_TOKEN) so the push triggers the
# alpha release workflow.

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  sync:
    name: Push main → alpha
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
          # PAT is required — GITHUB_TOKEN pushes don't trigger workflows.
          token: ${{ secrets.PAT_TOKEN }}

      - name: Push to alpha
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git push origin main:alpha --force
